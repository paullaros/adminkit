# AdminKit v4 Free SPIKE notes

## Goal

Replace Webpack + Babel polyfill + hand-maintained Free HTML with Vite + Eleventy 3/Nunjucks + Chart.js 4. Zero jQuery.

- `src/data/nav-free.json` drives sidebar. Pro will add `nav-pro.json`.
- Chart.js 4 uses `Chart.defaults.color` / `font` (not `defaults.global`).

See BUILD-REPORT.md for build results.
