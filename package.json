{
  "name": "adminkit-v4-free-spike",
  "version": "4.0.0-spike",
  "private": true,
  "description": "AdminKit Free v4 toolchain SPIKE — Vite + Eleventy/Nunjucks, Chart.js 4, zero jQuery",
  "type": "module",
  "scripts": {
    "dev": "npm run build:assets && concurrently -k -n pages,vite -c blue,magenta \"npm:pages:watch\" \"npm:assets:watch\"",
    "pages:watch": "eleventy --serve --port=8080 --watch",
    "assets:watch": "vite build --watch --emptyOutDir false",
    "pages:free": "eleventy",
    "build:assets": "vite build --emptyOutDir false",
    "build": "node scripts/clean-dist.mjs && npm run build:assets && npm run pages:free",
    "clean": "node scripts/clean-dist.mjs"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.8",
    "chart.js": "^4.4.8",
    "feather-icons": "^4.29.2",
    "flatpickr": "^4.6.13",
    "jsvectormap": "^1.7.0",
    "simplebar": "^5.3.9"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "concurrently": "^9.1.2",
    "nunjucks": "^3.2.4",
    "sass": "^1.85.0",
    "vite": "^6.2.0"
  }
}
